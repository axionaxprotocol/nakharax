# Retired: EU VPS Dashboard Deployment

The former VPS is decommissioned. The dashboard has no assigned production host or domain yet. Configure it only after the new infrastructure and domain are approved, and connect it through the verified HTTPS RPC domain rather than a direct VPS IP.

Core network deployment is documented in [1_SEP_GENESIS_RUNBOOK.md](../ops/1_SEP_GENESIS_RUNBOOK.md).
