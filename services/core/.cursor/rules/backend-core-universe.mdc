---
description: Backend development rules (Core Universe) — Rust, Python DeAI, ops, scripts
globs: core/**/*, ops/**/*, tools/**/*, scripts/**/*, configs/**/*, *.toml, hydra_manager.py
alwaysApply: false
---

# AXIONAX BACKEND — Core Universe

This repo is `axionax-core-universe`: Rust blockchain core + Python DeAI + ops + tools.  
**Version:** Core v1.9.0 | Tests: 201/201 | Chain ID: 86137 (testnet)

## Repository Layout

```
axionax-core-universe/
├── core/                  ← Rust Cargo workspace (core/Cargo.toml)
│   ├── core/<crate>/      ← 18 Rust crates (consensus, blockchain, node, rpc, ...)
│   ├── bridge/rust-python/← PyO3 FFI
│   ├── tools/faucet/      ← Testnet faucet
│   └── deai/              ← Python worker layer
├── configs/               ← TOML configs (monolith_*.toml)
├── ops/deploy/            ← Docker Compose environments
├── ops/scripts/           ← Monitoring scripts (Python)
└── scripts/               ← Node operation scripts
```

## Rust — Commands (always run from `core/`)

```bash
cargo build --release
cargo test --workspace           # must pass — 201/201
cargo clippy --workspace -- -D warnings   # zero warnings
cargo fmt --all
cargo audit                      # security check
```

- Workspace root is `core/Cargo.toml` — never restructure without direction
- All crates live under `core/core/` (exception: `bridge/`, `tools/`)
- See `rust-core.mdc` for deep Rust rules

## Python DeAI — Commands (run from `core/deai/`)

```bash
python -m pytest . -v --tb=short --ignore=tests
python core/deai/worker_node.py                                    # general worker
python core/deai/worker_node.py --config configs/monolith_scout_single.toml  # Monolith
python hydra_manager.py                                             # HYDRA dual-core
```

- Python 3.10+ only
- Dependencies: `core/deai/requirements.txt`
- Config: `worker_config.toml` (override with `--config`)
- Secrets: `.env` (from `.env.example`) — **never commit `.env` or `worker_key.json`**
- See `python-deai.mdc` for deep Python rules

## Ops Scripts (`ops/scripts/`)

| Script | Purpose |
|---|---|
| `health_monitor.py` | Automated health alerting (email on failure) |
| `validator_monitor.py` | Validator set + block production monitoring |
| `rpc_benchmark.py` | Load/concurrency benchmark |
| `rate_limit_dashboard.py` | Real-time rate limiting dashboard |
| `dev_server.py` | Mock JSON-RPC + health server (no Rust compile) |

## Docker Environments

| File | Use Case |
|---|---|
| `docker-compose.dev.yml` | Local dev |
| `ops/deploy/docker-compose.vps.yml` | VPS production |
| `ops/deploy/environments/testnet/public/docker-compose.yaml` | Public testnet |

**Services:** `validator`, `rpc-node`, `faucet`, `mock-rpc`, `prometheus`, `grafana`

## Node Configs

| Config | Description |
|---|---|
| `core/deai/worker_config.toml` | General PC/Server worker |
| `configs/monolith_scout_single.toml` | Monolith Scout (single Hailo-10H) |
| `configs/monolith_sentinel.toml` | HYDRA Sentinel (Hailo #0) |
| `configs/monolith_worker.toml` | HYDRA Worker (Hailo #1) |
| `core/configs/protocol.testnet.yaml` | Canonical protocol parameters |

## Live Testnet

| Validator | IP | RPC |
|---|---|---|
| #1 (EU) | `217.216.109.5` | `https://rpc.axionax.org` |
| #2 (AU) | `46.250.244.4` | `https://rpc-au.axionax.org` |

## Security Reminders

- `.env`, `worker_key.json`, private keys → always in `.gitignore`, never committed
- `WORKER_PRIVATE_KEY` env injection is the production pattern (not file-based)
- RPC authentication via `X-API-Key` header when `AXIONAX_RPC_API_KEY` is set
- CORS restricted via `AXIONAX_RPC_CORS_ORIGINS` env
