---
description: Frontend development rules (Web Universe) — Next.js, React, SDK in web-universe submodule
globs: web-universe/**/*
alwaysApply: false
---

# NAKHARAX FRONTEND — Web Universe (Git Submodule)

The frontend project lives at **web-universe/** (Git submodule pointing to `nakharax-monolith` repo).  
Stack: Next.js 14, TypeScript, Tailwind CSS, pnpm workspaces, TurboRepo

## Structure (inside `web-universe/`)

```
nakharax-monolith/
├── apps/
│   ├── web/           ← Next.js 14 App Router — main portal, explorer, staking UI
│   └── marketplace/   ← Vite + React — compute job marketplace
└── packages/
    ├── sdk/           ← @nakharax/sdk — RPC calls, wallet, ABI, types, contract wrappers
    └── ui/            ← @nakharax/ui — shared design system (Tailwind + Radix-based)
```

## Rules

### Always
- Use **pnpm** — never `npm` or `yarn` at the web-universe root
- Use workspace aliases: `import { ... } from "@nakharax/sdk"` — never relative cross-package paths
- Default to **Server Components** in Next.js; add `"use client"` only for hooks (`useState`, `useEffect`, wallet events)
- **All blockchain logic** (RPC, ABI, contract calls, wallet) goes in `packages/sdk` — apps only consume it
- TypeScript strict mode — no `any`, no `@ts-ignore`
- Use `NEXT_PUBLIC_*` env vars for client-side config only

### Never
- Never put reusable business logic in `apps/web` or `apps/marketplace` — move to `packages/sdk`
- Never hardcode API keys, private keys, or RPC credentials in frontend code
- Never use `npm install` or `yarn add` — always `pnpm --filter <pkg> add <dep>`
- Never import types from `apps/` into `packages/` (one-way dependency)

## Chain Config for Frontend

```typescript
export const NAKHARAX_TESTNET_CONFIG = {
  chainId: 86137,
  chainIdHex: "0x15079",
  name: "Nakharax Testnet",
  rpcUrl: process.env.NEXT_PUBLIC_RPC_URL ?? "https://rpc.nakharaxx.io",
  currency: { name: "NAK", symbol: "NAK", decimals: 18 },
  explorerUrl: process.env.NEXT_PUBLIC_EXPLORER_URL ?? "",
}
```

## Styling
- Tailwind CSS — dark mode default, cyberpunk/futuristic aesthetic
- Dark bg: `#0a0a0f` / `#0d0d1a`; Accent: cyan (`#00f5ff`) and purple (`#a855f7`)
- Framer Motion for animations; Lucide React for icons; shadcn/ui for components

## Commands

```bash
# Run from web-universe/ root
pnpm install
pnpm dev                          # starts all apps in parallel
pnpm --filter apps/web dev        # web only
pnpm --filter apps/marketplace dev  # marketplace only
pnpm build                        # production build
pnpm lint
```
