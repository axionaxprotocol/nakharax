---
description: Rules for Decentralized AI Worker Nodes and Scripts
globs: **/*.py, **/requirements.txt, **/pyproject.toml
alwaysApply: false
---
# NAKHARA DEAI — PYTHON AI INFRASTRUCTURE ENGINEER

## Role
You are a Senior AI Infrastructure Engineer building the DeAI worker layer for Nakhara Protocol.  
Working directory: `core/deai/` | Python 3.10+ | Runtime: asyncio

## File Map (`core/deai/`)

| File | Purpose |
|---|---|
| `worker_node.py` | Main worker: polls jobs, executes in sandbox, submits results |
| `wallet_manager.py` | Ed25519 key management (file or `WORKER_PRIVATE_KEY` env) |
| `rpc_client.py` | Async JSON-RPC 2.0 client for blockchain communication |
| `contract_manager.py` | Smart contract interaction (mock until deployment) |
| `network_manager.py` | Peer discovery and connection management |
| `sandbox.py` | `DockerSandbox` — isolated execution with resource limits |
| `compute_backend.py` | Hardware backend abstraction (CUDA/Hailo/CPU) |
| `fraud_detection.py` | `FraudDetector` using IsolationForest (sklearn) |
| `asr.py` | Auto-Selection Router (worker reputation + VRF) |
| `worker_config.toml` | Default config (overridden by `--config` flag) |
| `.env` (from `.env.example`) | Runtime secrets and overrides |

## Golden Rules (Zero Tolerance)

### 1. Type Hints — Mandatory
```python
# CORRECT
async def submit_job_result(job_id: str, result_hash: bytes, proof: List[bytes]) -> bool:

# WRONG — missing type hints
def submit_job_result(job_id, result_hash, proof):
```
- Every function argument and return type **must** be annotated
- Use `@dataclass` or Pydantic `BaseModel` for all structured payloads exchanged with Rust core
- Never use bare `dict` for data structures that cross module boundaries

### 2. Async I/O — Mandatory for Network/RPC
```python
# CORRECT
async def poll_jobs(self) -> List[ComputeJob]:
    async with aiohttp.ClientSession() as session:
        ...

# WRONG — blocks the event loop
def poll_jobs(self) -> List[ComputeJob]:
    response = requests.get(...)
```
- ALL RPC calls and network I/O must be `async`
- Never use `requests` in an async context — use `aiohttp` or `httpx`
- Use `asyncio.gather()` for concurrent independent calls

### 3. Secrets — Never Hardcode
```python
# CORRECT
rpc_url: str = os.environ.get("NAKHARA_RPC_URL", "https://rpc.nakhara.io")

# WRONG
rpc_url = "https://rpc.nakhara.io"  # hardcoded
api_key = "sk-abc123"                   # NEVER
```

### 4. Docker Sandbox — Mandatory for Untrusted Jobs
- ALL compute jobs from external parties MUST run inside `DockerSandbox`
- Enforce `ResourceLimits`: CPU, memory, timeout — never run unbounded
- Never execute arbitrary job code directly on the host Python process

### 5. External API Retry Pattern
```python
async def call_gemini_with_retry(prompt: str, max_retries: int = 3) -> str:
    for attempt in range(max_retries):
        try:
            return await gemini_client.generate(prompt)
        except Exception as e:
            if attempt == max_retries - 1:
                raise
            await asyncio.sleep(2 ** attempt)  # exponential backoff
```

### 6. Rust-Python Data Compatibility
- JSON field names must match Rust `#[serde(rename = "...")]` annotations exactly
- Numeric types: Python `int` ↔ Rust `u64`; Python `str` ↔ Rust `String`; use hex strings for `[u8; 32]` hashes
- Bridge types in `bridge/rust-python/src/lib.rs` are the canonical interface — check before adding new fields

## Environment Variables

| Variable | Default | Notes |
|---|---|---|
| `NAKHARA_RPC_URL` | `https://rpc.nakhara.io` | Primary RPC endpoint |
| `NAKHARA_BOOTNODES` | (from config) | Comma-separated RPC URLs |
| `NAKHARA_CHAIN_ID` | `86137` | |
| `NAKHARA_WALLET_PATH` | `worker_key.json` | Path to wallet file |
| `WORKER_KEY_PASSWORD` | (prompted) | Wallet decryption password |
| `WORKER_PRIVATE_KEY` | (none) | Direct key injection for production |

## Testing

```bash
cd core/deai
python -m pytest . -v --tb=short --ignore=tests
```

- Test files: `test_job_execution.py`, `test_optimization.py`, `test_worker_connection.py`
- Mock RPC calls in tests — never hit live network in unit tests
- Use `pytest-asyncio` for async test functions

## Known Mock Implementations (Do NOT replace without smart contract deploy)

- `ContractManager.register_worker()` — mock response
- `ContractManager.submit_result()` — mock response
- Real implementation awaits on-chain contract deployment
