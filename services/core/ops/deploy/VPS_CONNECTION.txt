# nakharax VPS Quick Commands
# Updated: 2026-05-24 — AU all-in-one services

# ── EU: Validator #1 + Nakharax OS (apps/os-dashboard :3030) ──
#     DNS: app.nakharax.io → 217.216.109.5
ssh root@217.216.109.5
# Deploy OS: bash apps/os-dashboard/scripts/vps-deploy.sh
# Docs: docs/web/VPS_EU_OS_DASHBOARD.md

# ── AU Validator #2 + all public services ──
#   rpc, explorer, api, faucet, nginx (docker-compose.vps.yml)
ssh root@46.250.244.4

# Upload deploy bundle to AU
scp -r services/core/ops/deploy root@46.250.244.4:/opt/nakharax/

# On AU (46.250.244.4)
cd /opt/nakharax
cp .env.example .env && nano .env
# VPS_IP=46.250.244.4
mv nginx/conf.d/explorer.conf.disabled nginx/conf.d/explorer.conf
docker compose -f docker-compose.vps.yml up -d
bash scripts/check-vps-status.sh --detailed

# See: VPS_AU_ALL_IN_ONE.md
