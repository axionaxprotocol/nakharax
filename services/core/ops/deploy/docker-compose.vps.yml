services:
  # RPC Node (behind nginx — not exposed directly)
  rpc-node:
    image: ghcr.io/axionaxprotocol/axionax-core:latest
    pull_policy: never
    container_name: axionax-rpc
    restart: unless-stopped
    ulimits:
      nofile: { soft: 65536, hard: 65536 }
    mem_limit: 2g
    cpus: 2.0
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"
    expose:
      - "8545"
      - "8546"
    ports:
      - "30303:30303/tcp"
      - "30303:30303/udp"
      - "8545:8545"
    environment:
      - RUST_LOG=info
      - RPC_HOST=0.0.0.0
      - RPC_PORT=8545
      - WS_PORT=8546
      - NETWORK=testnet
      - ENABLE_CORS=true
      - CORS_ORIGINS=https://axionax.org,https://www.axionax.org,https://explorer.axionax.org,https://faucet.axionax.org,https://api.axionax.org
    volumes:
      - rpc-data:/data
      - ./configs/rpc-config.toml:/etc/axionax/config.toml:ro
    networks:
      - axionax-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8545/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Block Explorer Backend (behind nginx — not exposed directly)
  explorer-backend:
    image: ghcr.io/axionaxprotocol/axionax-explorer:latest
    pull_policy: never
    container_name: axionax-explorer-backend
    restart: unless-stopped
    mem_limit: 1g
    cpus: 1.0
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"
    expose:
      - "3001"
    environment:
      - NODE_ENV=production
      - RPC_URL=http://rpc-node:8545
      - DATABASE_URL=postgresql://explorer:${DB_PASSWORD}@postgres:5432/explorer
      - PORT=3001
    depends_on:
      - rpc-node
      - postgres
    networks:
      - axionax-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3001/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Testnet Faucet (behind nginx — not exposed directly)
  faucet:
    image: ghcr.io/axionaxprotocol/axionax-faucet:latest
    pull_policy: never
    container_name: axionax-faucet
    restart: unless-stopped
    mem_limit: 512m
    cpus: 0.5
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "3"
    expose:
      - "3002"
    environment:
      - NODE_ENV=production
      - RPC_URL=http://rpc-node:8545
      - FAUCET_PRIVATE_KEY=${FAUCET_PRIVATE_KEY}
      - FAUCET_AMOUNT=100
      - RATE_LIMIT_MINUTES=1440  # 24 hours
      - PORT=3002
      - CHAIN_ID=86137
      - REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379
    depends_on:
      - rpc-node
      - redis
    networks:
      - axionax-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3002/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # PostgreSQL Database (internal only — never expose publicly)
  postgres:
    image: postgres:16-alpine
    container_name: axionax-postgres
    restart: unless-stopped
    mem_limit: 1g
    cpus: 1.0
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"
    environment:
      - POSTGRES_DB=explorer
      - POSTGRES_USER=explorer
      - POSTGRES_PASSWORD=${DB_PASSWORD}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - axionax-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U explorer"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis Cache (internal only — never expose publicly)
  redis:
    image: redis:7-alpine
    container_name: axionax-redis
    restart: unless-stopped
    command: ["redis-server", "--requirepass", "${REDIS_PASSWORD:?Set in .env}"]
    ports:
      - "127.0.0.1:6379:6379"
    cpus: 0.5
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "3"
    volumes:
      - redis-data:/data
    networks:
      - axionax-net
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Nginx Reverse Proxy
  nginx:
    image: nginx:alpine
    container_name: axionax-nginx
    restart: unless-stopped
    mem_limit: 256m
    cpus: 0.5
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - ./certbot/conf:/etc/letsencrypt:ro
      - ./certbot/www:/var/www/certbot:ro
    depends_on:
      rpc-node:
        condition: service_started
      explorer-backend:
        condition: service_started
      faucet:
        condition: service_started
    networks:
      - axionax-net
    healthcheck:
      test: ["CMD", "nginx", "-t"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Certbot for SSL
  certbot:
    image: certbot/certbot
    container_name: axionax-certbot
    volumes:
      - ./certbot/conf:/etc/letsencrypt
      - ./certbot/www:/var/www/certbot
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"

  # Prometheus Monitoring (internal only — access via Grafana)
  prometheus:
    image: prom/prometheus:latest
    container_name: axionax-prometheus
    restart: unless-stopped
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
    networks:
      - axionax-net

  # Grafana Dashboard (localhost only — SSH tunnel for admin access)
  grafana:
    image: grafana/grafana:latest
    container_name: axionax-grafana
    restart: unless-stopped
    ports:
      - "127.0.0.1:3030:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD}
      - GF_INSTALL_PLUGINS=grafana-clock-panel
    volumes:
      - grafana-data:/var/lib/grafana
      - ./monitoring/grafana/dashboards:/etc/grafana/provisioning/dashboards:ro
      - ./monitoring/grafana/datasources:/etc/grafana/provisioning/datasources:ro
    depends_on:
      - prometheus
    networks:
      - axionax-net

volumes:
  rpc-data:
  postgres-data:
  redis-data:
  prometheus-data:
  grafana-data:

networks:
  axionax-net:
    driver: bridge
