# Copy to .env.vps3-faucet on VPS3 and set FAUCET_PRIVATE_KEY.
# Faucet address must match genesis (create_genesis.py default = deterministic key).
# Get default key hex: python3 -c "import hashlib; print(hashlib.sha256(b'nakhara_faucet_mainnet_q2_2026').hexdigest())"

FAUCET_PRIVATE_KEY=
