# Copy outside the repository, for example /etc/nakharax-monitoring/monitoring.env.
# Use a unique Grafana password and an operations webhook that accepts the
# standard Alertmanager JSON payload. Do not commit the resulting file.
GRAFANA_ADMIN_PASSWORD=replace-with-a-long-unique-password
ALERT_WEBHOOK_URL=https://alerts.example.invalid/nakharax
