# Nakhara Core Universe - Full Stack Development
# Runs blockchain core + web services together

services:
  # ============================================
  # 🦀 Blockchain Node (Primary)
  # ============================================
  nakhara-node:
    build:
      context: ./core
      dockerfile: ops/deploy/Dockerfile
    container_name: nakhara-node
    restart: unless-stopped
    ports:
      - "8545:8545"     # JSON-RPC HTTP
      - "8546:8546"     # JSON-RPC WebSocket
      - "30303:30303"   # P2P
      - "9100:9100"     # Prometheus metrics
    volumes:
      - node_data:/data
    environment:
      - RUST_LOG=info,nakhara=debug
      - CHAIN_ID=86137
      - NETWORK=testnet
      - RPC_HOST=0.0.0.0
      - RPC_PORT=8545
      - WS_PORT=8546
      - P2P_PORT=30303
      - BLOCK_TIME=5
      - MAX_PEERS=50
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8545", "-X", "POST", "-H", "Content-Type: application/json", "-d", "{\"jsonrpc\":\"2.0\",\"method\":\"eth_blockNumber\",\"params\":[],\"id\":1}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - nakhara-core

  # ============================================
  # 🦀 Second Validator Node
  # ============================================
  nakhara-validator:
    build:
      context: ./core
      dockerfile: ops/deploy/Dockerfile
    container_name: nakhara-validator
    restart: unless-stopped
    ports:
      - "8555:8545"     # JSON-RPC HTTP (different port)
      - "30304:30303"   # P2P
    volumes:
      - validator_data:/data
    environment:
      - RUST_LOG=info
      - CHAIN_ID=86137
      - NETWORK=testnet
      - BOOTNODES=enode://node@nakhara-node:30303
      - VALIDATOR_MODE=true
    depends_on:
      - nakhara-node
    networks:
      - nakhara-core

  # ============================================
  # 🗄️ PostgreSQL Database
  # ============================================
  postgres:
    image: postgres:16-alpine
    container_name: nakhara-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: nakhara
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set in .env}
      POSTGRES_DB: nakhara_testnet
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nakhara -d nakhara_testnet"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - nakhara-core

  # ============================================
  # 📦 Redis Cache
  # ============================================
  redis:
    image: redis:7-alpine
    container_name: nakhara-redis
    restart: unless-stopped
    command: ["redis-server", "--requirepass", "${REDIS_PASSWORD:?Set in .env}"]
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - nakhara-core

  # ============================================
  # 🌐 Web Interface (from web-universe)
  # ============================================
  web:
    build:
      context: ./web-universe/apps/web
      dockerfile: Dockerfile
    container_name: nakhara-web
    restart: unless-stopped
    depends_on:
      - nakhara-node
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_RPC_URL=http://nakhara-node:8545
      - NEXT_PUBLIC_WS_URL=ws://nakhara-node:8546
      - NEXT_PUBLIC_CHAIN_ID=86137
      - NODE_ENV=development
    networks:
      - nakhara-core

  # ============================================
  # 🛒 Marketplace (from web-universe)
  # ============================================
  marketplace:
    build:
      context: ./web-universe/apps/marketplace
      dockerfile: Dockerfile
    container_name: nakhara-marketplace
    restart: unless-stopped
    depends_on:
      - nakhara-node
    ports:
      - "5173:5173"
    environment:
      - VITE_RPC_URL=http://nakhara-node:8545
      - VITE_CHAIN_ID=86137
    networks:
      - nakhara-core

  # ============================================
  # 💧 Faucet API (built from core/tools/faucet)
  # ============================================
  faucet:
    build:
      context: ./core
      dockerfile: ops/deploy/Dockerfile.faucet
    container_name: nakhara-faucet
    restart: unless-stopped
    depends_on:
      nakhara-node: { condition: service_healthy }
      redis: { condition: service_started }
    ports:
      - "3002:3002"
    environment:
      - RPC_URL=http://nakhara-node:8545
      - PORT=3002
      - CHAIN_ID=86137
      # Dev only: use a test key; production must set FAUCET_PRIVATE_KEY securely
      - FAUCET_PRIVATE_KEY=${FAUCET_PRIVATE_KEY:?Set in .env}
    networks:
      - nakhara-core
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:3002/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s

  # ============================================
  # 📊 Prometheus (Monitoring)
  # ============================================
  prometheus:
    image: prom/prometheus:v2.48.0
    container_name: nakhara-prometheus
    restart: unless-stopped
    ports:
      - "9090:9090"
    volumes:
      - ./ops/deploy/monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
    networks:
      - nakhara-core

  # ============================================
  # 📈 Grafana (Dashboards)
  # ============================================
  grafana:
    image: grafana/grafana:10.2.2
    container_name: nakhara-grafana
    restart: unless-stopped
    depends_on:
      - prometheus
    ports:
      - "3030:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD:?Set in .env}
    volumes:
      - grafana_data:/var/lib/grafana
      - ./ops/deploy/monitoring/grafana/dashboards:/etc/grafana/provisioning/dashboards:ro
    networks:
      - nakhara-core

  # ============================================
  # 🔍 Block Explorer Backend (stub in dev; use real explorer in production)
  # ============================================
  explorer-api:
    build:
      context: ./tools/devtools
      dockerfile: Dockerfile.explorer
    container_name: nakhara-explorer-api
    restart: unless-stopped
    depends_on:
      - nakhara-node
      - postgres
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
    networks:
      - nakhara-core
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:3001/api/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 5s

volumes:
  node_data:
  validator_data:
  postgres_data:
  redis_data:
  prometheus_data:
  grafana_data:

networks:
  nakhara-core:
    name: nakhara-core-network
    driver: bridge
